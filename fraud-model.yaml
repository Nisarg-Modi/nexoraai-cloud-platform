apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: fraud-model
  namespace: mlops
spec:
  predictor:
    serviceAccountName: default
    model:
      modelFormat:
        name: sklearn
      storageUri: gs://<your-bucket>/fraud-model/
